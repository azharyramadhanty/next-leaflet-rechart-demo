export enum BunkerType {
    LFO = "LFO",
    HFO = "HFO",
    MGO = "MGO",
    MFO = "MFO",
    MDF = "MDF",
    HSD = "HSD",
}

export enum BunkerConfirmModalType {
    COMPLETED = "completed",
    CANCELED = "canceled"
}

export enum BunkerConfirmModalCloseType {
    CLOSED = "closed",
    SAVED = "saved"
}